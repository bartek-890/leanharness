@CLAUDE.md

This project's agent instructions live in `CLAUDE.md` — one source of truth
for every coding agent. If your tool reads `AGENTS.md` natively, read
`CLAUDE.md` and treat it as the complete rule set.
